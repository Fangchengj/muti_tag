<%= form_for(tag_one) do |f| %>
  <% if tag_one.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_one.errors.count, "error") %> prohibited this tag_one from being saved:</h2>

      <ul>
      <% tag_one.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "提交" %>
  </div>
<% end %>
